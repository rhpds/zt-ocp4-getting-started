= Lab 1: Deploy the App -- 3 Ways to Get Code Running
:source-highlighter: rouge
:toc: macro
:toclevels: 2

There is no single right way to get an application onto OpenShift -- and this lab proves it.
You'll deploy the DataSphere application three different ways: from a pre-built container image,
from source code using Source-to-Image, and as a complete multi-component Helm chart. Along
the way you'll learn the three building blocks that make every OpenShift application work: *Pods*,
*Services*, and *Routes*.

By the end, you'll have a fully working DataSphere instance -- UI, API, and database --
deployed from a single Helm chart, and you'll understand exactly what each piece does.

toc::[]

== Learning objectives

By the end of this module, you'll be able to:

* Identify and describe the role of Pods, Services, and Routes in OpenShift
* Deploy a containerized application from a pre-built image using the `oc` CLI
* Build and deploy an application from source code using Source-to-Image (S2I)
* Deploy a multi-component application in a single command using a Helm chart
* Explain when to reach for each of the three deployment methods

== Get connected

=== OpenShift web console

The OpenShift web console gives you a visual interface to explore and manage your cluster.
To open it, click the *Console* tab on the right side of this lab interface.

Log in with these credentials:

* *Username*: `{user}`
* *Password*: `{password}`
* *Console URL*: {openshift_console_url}

=== Terminal (CLI)

Most steps in this lab use the `oc` command-line tool. To open a terminal, click the *Terminal* tab
on the right side of this lab interface. The `oc` CLI is pre-installed and ready to use.

Log in to the cluster:

[source,role="execute",subs="attributes+"]
----
oc login {openshift_api_url} --username {user} --password {password}
----

[%collapsible]
.Expected output
====
[subs="attributes+"]
----
Login successful.

You have access to the following projects and can switch between them with 'oc project <projectname>':

  * {user}-datasphere
    {user}-lab

Using project "{user}-datasphere".
Welcome! See 'oc help' to get started.
----
====

Confirm you're logged in:

[source,role="execute"]
----
oc whoami
----

[%collapsible]
.Expected output
====
[subs="attributes+"]
----
{user}
----
====

=== Your lab projects

Two projects have been provisioned for you:

* `{user}-datasphere`: contains the pre-deployed DataSphere demo app -- explore it in Section 1
* `{user}-lab`: your clean workspace -- you'll build here in Section 2 and Section 3

NOTE: In OpenShift, a Project is equivalent to a Kubernetes namespace. It scopes your resources and controls access.

== Section 1: See the finished application (5 minutes)

The DataSphere demo app is already running in your `{user}-datasphere` project.
It was deployed automatically when your lab environment was provisioned.
Before you build anything, let's see what the finished result looks like -- and understand
what makes it work.

=== Step 1.1: Open the application

[source,role="execute",subs="attributes+"]
----
oc project {user}-datasphere
----

Get the application URL:

[source,role="execute"]
----
oc get route datasphere-ui
----

Copy the URL from the `HOST` column and open it in your browser. You'll see a live map of global datacenter sites.
Click a site to toggle its status and watch the map respond in real time.

That's a two-tier application -- a React frontend backed by a Python API -- running on OpenShift,
accessible from anywhere on the internet.

Now let's understand what made that possible.

=== Step 1.2: See what's running

[source,role="execute"]
----
oc get all
----

[%collapsible]
.Expected output
====
----
NAME                                  READY   STATUS    RESTARTS   AGE
pod/datasphere-api-7d6f9b8c4-xk2pq   1/1     Running   0          5m
pod/datasphere-ui-5c8b4f6d9-n7rlv    1/1     Running   0          5m

NAME                     TYPE        CLUSTER-IP       EXTERNAL-IP   PORT(S)    AGE
service/datasphere-api   ClusterIP   172.30.14.22     <none>        8080/TCP   5m
service/datasphere-ui    ClusterIP   172.30.198.47    <none>        8080/TCP   5m

NAME                             READY   UP-TO-DATE   AVAILABLE   AGE
deployment.apps/datasphere-api   1/1     1            1           5m
deployment.apps/datasphere-ui    1/1     1            1           5m

NAME                                        DESIRED   CURRENT   READY   AGE
replicaset.apps/datasphere-api-7d6f9b8c4   1         1         1       5m
replicaset.apps/datasphere-ui-5c8b4f6d9    1         1         1       5m

NAME                                   HOST/PORT
route.route.openshift.io/datasphere-ui datasphere-ui-{user}-datasphere.apps.{cluster_subdomain}
----
====

Notice: two Pods, two Services, two Deployments -- but only one Route. Both components have a Service
for internal communication, but only the frontend has a Route for external access.
Let's explore why.

=== Step 1.3: Explore the Pod

A *Pod* is the smallest deployable unit in OpenShift. It wraps one or more containers and gives
them a shared network and storage context.

List the pods in your project:

[source,role="execute"]
----
oc get pods
----

[%collapsible]
.Expected output
====
----
NAME                              READY   STATUS    RESTARTS   AGE
datasphere-api-7d6f9b8c4-xk2pq   1/1     Running   0          5m
datasphere-ui-5c8b4f6d9-n7rlv    1/1     Running   0          5m
----
====

Inspect the frontend pod in detail. The `-l component=ui` flag selects pods by label instead of
by name, so you don't need to know the exact pod name:

[source,role="execute"]
----
oc describe pod -l component=ui
----

[%collapsible]
.Expected output (key fields)
====
----
Name:         datasphere-ui-5c8b4f6d9-n7rlv
Namespace:    {user}-datasphere
Labels:       app=datasphere
Containers:
  datasphere-ui:
    Image:    {datasphere_ui_image}
    Port:     8080/TCP
    State:    Running
    Ready:    True
Conditions:
  Ready:      True
----
====

Notice the output shows key information about the pod: the container image it's running,
the ports it exposes, its current status, and the labels that connect it to other resources.

Check the application logs:

[source,role="execute"]
----
oc logs -l component=ui
----

[%collapsible]
.Expected output (truncated)
====
----
10.x.x.x - - [31/Mar/2026:12:00:00 +0000] "GET / HTTP/1.1" 200 ...
10.x.x.x - - [31/Mar/2026:12:00:00 +0000] "GET /assets/index.js HTTP/1.1" 200 ...
----
====

NOTE: If you see no output, that's normal. The web server only writes log entries when it
receives requests. Open the application URL in your browser, then run the command again.

NOTE: Pods are ephemeral. They can be created, destroyed, and replaced at any time.
A *Deployment* manages this lifecycle and ensures the right number of pods are always running.

=== Step 1.4: Explore the Service

A *Service* gives your application a stable internal network address. Even if the Pod restarts
and gets a new IP address, the Service address stays the same.

[source,role="execute"]
----
oc get service datasphere-ui
----

[%collapsible]
.Expected output
====
----
NAME            TYPE        CLUSTER-IP      EXTERNAL-IP   PORT(S)    AGE
datasphere-ui   ClusterIP   172.30.198.47   <none>        8080/TCP   5m
----
====

[source,role="execute"]
----
oc describe service datasphere-ui
----

[%collapsible]
.Expected output (key fields)
====
----
Name:              datasphere-ui
Selector:          app=datasphere,component=ui
Type:              ClusterIP
Port:              8080/TCP
Endpoints:         10.x.x.x:8080
----
====

Notice the `Selector` field: it uses labels (`app=datasphere,component=ui`) to identify which pods to
route traffic to. This label-based selection is how OpenShift connects resources together.

=== Step 1.5: Explore the Route

A *Route* exposes a Service to traffic from outside the cluster. It provides a public hostname
and handles TLS termination so your users can access the app over HTTPS.

[source,role="execute"]
----
oc describe route datasphere-ui
----

[%collapsible]
.Expected output (key fields)
====
----
Name:            datasphere-ui
Host:            datasphere-ui-{user}-datasphere.apps.{cluster_subdomain}
TLS Termination: edge
Insecure Policy: Redirect
Service:         datasphere-ui
Port:            8080
----
====

Notice the `Host` field: that's the public URL you opened in your browser.
Notice the `TLS` section: OpenShift handles the certificate automatically.

=== Step 1.6: Why datasphere-api has no Route

Look back at the `oc get all` output from Step 1.2. `datasphere-api` has a Service but no Route.
That's deliberate: the API only needs to be reachable from within the cluster, not from the internet.

[source,role="execute"]
----
oc get service datasphere-api
----

[%collapsible]
.Expected output
====
----
NAME             TYPE        CLUSTER-IP     EXTERNAL-IP   PORT(S)    AGE
datasphere-api   ClusterIP   172.30.14.22   <none>        8080/TCP   5m
----
====

The UI talks to the API by Service name -- `datasphere-api` -- and OpenShift's internal DNS
resolves it automatically. No Route needed.

=== How they connect

[mermaid]
....
flowchart TB
    B["Browser"]
    R["Route\n(datasphere-ui)"]
    S["Service\n(stable internal IP)"]
    P1["Pod\n(datasphere-ui-n7rlv)"]
    P2["Pod\n(datasphere-ui-fgh12)"]:::dashed
    P3["Pod\n(datasphere-ui-zxv9k)"]:::dashed

    B -->|"HTTPS"| R
    R --> S
    S -->|"label selector"| P1
    S -->|"label selector"| P2
    S -->|"label selector"| P3

    classDef dashed stroke-dasharray: 5 5
....

The Route knows which Service to send traffic to.
The Service knows which Pods to forward requests to via label selectors.
The Pod runs your container.

The dashed pods aren't running right now, but if you scaled up, the Service would
automatically route traffic to every pod with a matching label.

[[section-2]]
== Section 2: Deploy it yourself -- two different ways (14 minutes)

Now that you understand the building blocks, it's time to deploy DataSphere yourself.
You'll use two different methods: first from a pre-built container image, then from
source code using Source-to-Image. Each method has its place, and by the end you'll
know when to reach for which.

TIP: If you're already familiar with deploying container images and S2I builds on OpenShift,
you can skip ahead to <<section-3>> where you'll deploy the full application with a Helm chart --
closer to how teams deploy in the real world.

=== Step 2.1: Switch to your clean project

[source,role="execute",subs="attributes+"]
----
oc project {user}-lab
----

This project is empty -- a clean workspace for you to build in.

=== Deploy the UI from a container image

The fastest way to deploy an application is from an image that's already built and published
to a registry. You just point OpenShift at the image and it handles the rest.

=== Step 2.2: Deploy the application

Deploy the DataSphere UI from its container image:

[source,role="execute",subs="attributes+"]
----
oc new-app {datasphere_ui_image} --name=datasphere-ui
----

[%collapsible]
.Expected output
====
----
--> Found container image ... ({datasphere_ui_image})
    ...
--> Creating resources ...
    imagestream.image.openshift.io "datasphere-ui" created
    deployment.apps "datasphere-ui" created
    service "datasphere-ui" created
--> Success
    Application is not exposed. You can expose services to the outside world by executing one or more of the commands below:
     'oc create route edge --service=datasphere-ui'
    Run 'oc status' to view your app.
----
====

`oc new-app` created three resources: an ImageStream to track the image version, a Deployment to
manage your pods, and a Service for internal network access. The output even tells you the next
step -- the app isn't externally accessible yet.

Wait for the pod to come up:

[source,role="execute"]
----
oc rollout status deployment/datasphere-ui
----

[%collapsible]
.Expected output
====
----
deployment "datasphere-ui" successfully rolled out
----
====

NOTE: This same rollout process happens every time you deploy a new version of your application.
OpenShift creates new pods first, waits for them to be healthy, then scales down the old ones:
no downtime, no manual coordination.

=== Step 2.3: Verify the Service

A Service already exists -- `oc new-app` created it. You can verify:

[source,role="execute"]
----
oc get service datasphere-ui
----

[%collapsible]
.Expected output
====
----
NAME            TYPE        CLUSTER-IP      EXTERNAL-IP   PORT(S)    AGE
datasphere-ui   ClusterIP   172.30.x.x      <none>        8080/TCP   30s
----
====

The `ClusterIP` is the stable internal address. Other applications inside the cluster can reach
your app at this address, but it's not yet accessible from outside.

=== Step 2.4: Create a Route for external access

[source,role="execute"]
----
oc create route edge --service=datasphere-ui
----

[%collapsible]
.Expected output
====
----
route.route.openshift.io/datasphere-ui created
----
====

=== Step 2.5: Verify the deployment

[source,role="execute"]
----
oc get route datasphere-ui
----

Copy the URL from the `HOST` column and open it in your browser.

The DataSphere UI loads -- but the map is empty. No datacenter sites, no status data.

That's not a bug. The UI is running exactly as it should: it's already calling out to
`datasphere-api:8080`, looking for the backend. But there's no pod answering that address yet.
You've deployed the frontend. The backend doesn't exist in this project.

=== Deploy the API via Source-to-Image

Rather than deploying from a pre-built image, you can point OpenShift directly at a source
code repository. OpenShift detects the language, installs dependencies, builds a container
image, and deploys it -- all inside the cluster, with no Dockerfile required.
This is *Source-to-Image* (S2I).

=== Step 2.6: Find your Gitea repository

Your lab environment includes a personal Gitea instance with the DataSphere API source already
loaded. Open the Gitea console:

[source,role="execute",subs="attributes+"]
----
echo {gitea_url}
----

Log in with your lab credentials (`{user}` / `{password}`). You should see a `datasphere-api`
repository already in your account -- this is the source code you'll build from.

Confirm the repository URL:

[source,role="execute",subs="attributes+"]
----
echo {gitea_url}/{user}/datasphere-api
----

=== Step 2.7: Build and deploy the API from source

The `python~` prefix tells OpenShift two things: use the Python builder image, and build from
the source that follows. OpenShift clones the repository, detects `requirements.txt`,
installs dependencies, and packages everything into a container image -- all inside the cluster,
with no Dockerfile required.

[source,role="execute",subs="attributes+"]
----
oc new-app python~{gitea_url}/{user}/datasphere-api --name=datasphere-api
----

[%collapsible]
.Expected output
====
----
--> Found image ... (Python 3.12 on UBI 9)
    ...
--> Creating resources ...
    imagestream.image.openshift.io "python" created
    imagestream.image.openshift.io "datasphere-api" created
    buildconfig.build.openshift.io "datasphere-api" created
    deployment.apps "datasphere-api" created
    service "datasphere-api" created
--> Success
    Build scheduled, use 'oc logs -f buildconfig/datasphere-api' to track its progress.
----
====

Notice the `buildconfig` in the output -- that wasn't there when you deployed the UI from a
pre-built image. The BuildConfig is the S2I build definition. OpenShift is now cloning the
repository and running the build inside the cluster. Watch it happen:

[source,role="execute"]
----
oc logs -f buildconfig/datasphere-api
----

[%collapsible]
.Expected output (truncated)
====
----
Cloning "https://gitea.../datasphere-api" ...
STEP 1/9: FROM image-registry.openshift-image-registry.svc:5000/openshift/python:latest
...
Collecting fastapi
Collecting uvicorn
...
Successfully installed fastapi-0.115.0 uvicorn-0.31.0 ...
Pushing image image-registry.openshift-image-registry.svc:5000/{user}-lab/datasphere-api:latest ...
Push successful
----
====

That entire build ran inside the cluster: no Docker installed locally, no CI/CD pipeline
configured, no external registry account needed. OpenShift pulled the source from your Gitea
repository, installed dependencies, built and tagged the image, pushed it to its internal
registry, and is now deploying it -- all triggered by one `oc new-app` command.

Wait for the deployment:

[source,role="execute"]
----
oc rollout status deployment/datasphere-api
----

[%collapsible]
.Expected output
====
----
deployment "datasphere-api" successfully rolled out
----
====

=== Step 2.8: Watch the map come to life

Open the UI URL -- the same URL from before:

[source,role="execute"]
----
oc get route datasphere-ui -o jsonpath='{.spec.host}'
----

The map is now populated with datacenter sites. You didn't reconfigure the UI. You didn't update
any hostnames or IP addresses. The moment `datasphere-api` came online as a Service in this
project, the UI found it automatically.

This is OpenShift's internal DNS in practice: the Nginx proxy in the UI container is configured
to forward `/api/` requests to `datasphere-api:8080`. OpenShift resolves that name to the
Service, which routes to the pod. Add a backend, the frontend connects to it. That's it.

[[section-3]]
== Section 3: Deploy everything with a Helm chart (6 minutes)

You deployed DataSphere in two stages: the UI from a container image, the API from source.
That works, but in practice most teams don't run individual `oc new-app` commands one at a time.
They package their application as a *Helm chart* -- a single installable unit that deploys
every component together.

A chart is a directory of templated Kubernetes manifests plus a `values.yaml` file that controls
configurable inputs (image tags, theme, replica count, and so on). When you run `helm install`,
Helm renders those templates with your chosen values and submits all the resulting manifests to
the cluster in one shot. It also records what it installed as a named *release*, so it can
upgrade, roll back, or cleanly remove every resource it owns.

=== Step 3.1: Clean up the manual deployments

First, remove the resources you created manually in Section 2:

[source,role="execute"]
----
oc delete all --selector app=datasphere-ui
oc delete all --selector app=datasphere-api
----

Verify the project is clean:

[source,role="execute"]
----
oc get all
----

The project should show no application resources.

=== Step 3.2: Deploy the full application

Install the DataSphere chart -- this deploys UI, API, and Redis in one command:

[source,role="execute",subs="attributes+"]
----
helm install datasphere {gitea_url}/api/packages/gitea-admin/helm/datasphere-{datasphere_helm_version}.tgz
----

The first argument after `helm install` is the *release name* (`datasphere`). Helm uses it to
track this installation. The second argument is the chart location.

NOTE: The same chart is also available in the OpenShift *Developer Catalog*. The URL above points
at the Gitea instance in your lab environment, which hosts the chart alongside your source code
repositories.

[%collapsible]
.Expected output
====
[subs="attributes+"]
----
NAME: datasphere
LAST DEPLOYED: ...
NAMESPACE: {user}-lab
STATUS: deployed
REVISION: 1
----
====

`REVISION: 1` tells you this is the first install. Each `helm upgrade` increments the revision
and Helm keeps the history -- you can roll back to any previous revision if something goes wrong.

Wait for both components:

[source,role="execute"]
----
oc rollout status deployment/datasphere-ui && oc rollout status deployment/datasphere-api
----

[%collapsible]
.Expected output
====
----
deployment "datasphere-ui" successfully rolled out
deployment "datasphere-api" successfully rolled out
----
====

=== Step 3.3: Verify the application

Open the application:

[source,role="execute"]
----
oc get route datasphere-ui -o jsonpath='{.spec.host}'
----

Both tiers, fully operational, from a single command. Every resource you created across
Section 2 was created here in one shot -- with no opportunity for steps to be missed or
applied in the wrong order.

=== Step 3.4: See configuration in action

Want to see how configuration changes work? The chart supports a `theme` value that swaps the
map color palette. Try it without redeploying anything:

[source,role="execute",subs="attributes+"]
----
helm upgrade datasphere {gitea_url}/api/packages/gitea-admin/helm/datasphere-{datasphere_helm_version}.tgz --set theme=mars
----

Helm re-renders only the resources that changed, applies them, and rolls the affected pod.
Refresh the app in your browser -- the map shifts to the Mars color scheme. Roll it back
just as easily:

[source,role="execute",subs="attributes+"]
----
helm upgrade datasphere {gitea_url}/api/packages/gitea-admin/helm/datasphere-{datasphere_helm_version}.tgz
----

NOTE: To remove the entire application and every resource it created: `helm uninstall datasphere`

== Module summary

You deployed the DataSphere application three different ways and learned the building blocks
behind every OpenShift application.

*The three building blocks:*

* *Pod*: the running container; ephemeral and managed by a Deployment
* *Service*: stable internal network address; uses label selectors to find pods
* *Route*: external access with a public hostname and automatic TLS termination

*The three deployment methods:*

* *Container image*: fastest path when the image is already built and published;
  no build step, no source code required
* *Source-to-Image*: point OpenShift at a Git repository and it builds, packages,
  and deploys the application; no Dockerfile, no local tooling required
* *Helm chart*: package the entire multi-component application for repeatable,
  version-controlled deployments; install or remove the whole stack in one command

*Next steps:*

DataSphere is running. Lab 2 covers what happens when things break -- and how OpenShift
keeps your application available automatically with scaling and self-healing.
